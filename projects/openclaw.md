# OpenClaw

## Summary
Shared notes for the OpenClaw setup running on the GCP VM.

## Current Status
- OpenClaw package is on 2026.4.2
- Default model is `openai-codex/gpt-5.4`
- `openclaw-gateway.service` runs the gateway
- `openclaw-webpage.service` runs the webpage server on port 8090

## Notes
- This directory is indexed through OpenClaw memory search `extraPaths`
- Use one Markdown file per project for durable searchable context
